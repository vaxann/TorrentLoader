#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nt/bin/nt.js" "$@"
  ret=$?
else 
  node  "$basedir/../nt/bin/nt.js" "$@"
  ret=$?
fi
exit $ret
